# Cloudflare Turnstile Secret Keys
# CRITICAL: These are SECRET keys and must NEVER be committed to version control
# Add the actual values to your .env file (which should be in .gitignore)
# In production, these MUST be injected via CI/CD secret management

# Participant Registration Form Secret Key
TURNSTILE_SECRET_PARTICIPANT=your-secret-key-here

# Promoter Registration Form Secret Key
TURNSTILE_SECRET_PROMOTER=your-secret-key-here

# Note: Site keys (public) are already in the source code:
# - Participant: 0x4AAAAAACCjOLDn-vMrwViE (in TreasureHuntRegistration.tsx)
# - Promoter: 0x4AAAAAACCjPAPEx1KF6so2 (in PromoterSelfRegistration.tsx)
